# AniML 项目设计文档

> 一款面向 Android 的**可视化动画制作软件**，动画本身使用 HTML 来制作。用户在横屏手机上像剪辑视频一样管理多个 HTML 层、时间轴和摄像机，最终可导出独立的 HTML 或录制为视频。

---

## 1. 项目概述

- **产品名称**：AniML（Animation Markup Layer）
- **平台**：Android APK（Flutter 3.x + Dart 3）
- **默认方向**：强制横屏（`landscape`）
- **核心定位**：
  - 这是一款**动画制作软件**，不是单纯的 HTML 预览器。
  - 动画内容由 HTML/CSS/JS 驱动，每个可编辑单元本质上是一段 HTML。
  - 编辑器提供**层（Layer）、时间轴（Timeline）、摄像机（Camera）**等动画/视频软件常见的工作流。
  - 最终产物可以是：
    - 一份可独立运行的 HTML 文件；
    - 一段由摄像机拍摄渲染区得到的视频（MP4/GIF/WebM）。

---

## 2. 核心概念

| 概念 | 说明 |
|------|------|
| **渲染页（Render Page）** | 占据整个横屏手机屏幕的白色背景区域，是用户看到的全部工作区。 |
| **渲染区（Render Area）** | 渲染页中一块带有浅边框的矩形区域，真正用来显示/运行动画内容。 |
| **坐标系** | 以渲染区左上角为原点 `(0,0)`，X 轴向右，Y 轴向下，单位为逻辑像素 `px`。编辑时可在背景上叠加浅色网格和坐标轴。 |
| **像素大小** | 渲染区的宽、高可按像素精确设置，例如 `1920×1080`、`1280×720`、`1080×1080`。 |
| **长宽比** | 提供常用比例：`16:9`、`4:3`、`1:1`、`9:16`、`自定义`。 |
| **HTML 层（Layer）** | 每个导入或创建的 HTML 对应一个层。层号越小越先渲染（在底部），层号大的会覆盖在上层。 |
| **时间轴条带（Clip）** | 位于底部时间轴上的一段范围，表示某个 HTML 层从何时开始渲染、何时结束。同一条轨道上的条带不允许重叠。 |
| **摄像机（Camera）** | 工具箱中的录制工具，只拍摄渲染区内的内容，将动画输出为视频。 |
| **三种工具模式** | **搜索模式**（浏览/手势控制）、**更改模式**（编辑 HTML 内容）、**摄像机模式**（录制）。 |

---

## 3. 技术栈

| 层级 | 选型 | 说明 |
|------|------|------|
| UI 框架 | Flutter 3.x（Dart 3） | 跨平台、手势丰富、APK 构建成熟。 |
| HTML 渲染引擎 | `flutter_inappwebview` | 在 Flutter 内嵌高性能 WebView，可注入 JS、监听事件、调用 DOM API。 |
| 多层合成 | 单 WebView + 多 `<iframe>` / `<div class="layer">` | 每个 HTML 层作为一个独立文档或透明容器，按 `z-index` 叠加。 |
| 状态管理 | `flutter_riverpod` | 管理渲染区、层、时间轴、摄像机、WebView 控制器等。 |
| 文件/存储 | `file_picker`、`path_provider`、`shared_preferences` | 导入 HTML/资源、缓存项目、保存用户偏好。 |
| 动画时序 | 自定义 Timeline Model + CSS/JS 注入 | 编辑器维护条带与关键帧，最终编译为 CSS `@keyframes` 或 JS 动画。 |
| 摄像机录制 | 原生 MediaProjection / MediaCodec（或 `flutter_screen_recording` 等录制库） | 仅捕获渲染区可见区域，输出视频。 |
| 编译打包 | Dart 脚本 + 正则/简单解析 | 将多层 HTML、CSS、JS、图片等资源合并或内联，输出单一 HTML 或视频。 |

---

## 4. UI/UX 设计

### 4.1 页面结构

```
┌──────────────────────────────────────────────────────────────┐
│  系统状态栏（隐藏或沉浸，保留电量/时间）                        │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│                    ┌─────────────────────┐                   │
│                    │                     │                   │
│       渲染页        │     渲染区           │     属性/图层面板   │
│     (纯白背景)      │  (浅边框 + 网格坐标)  │    (可收起)        │
│                    │                     │                   │
│                    └─────────────────────┘                   │
│                                                              │
│  🔍  ✏️  📷                                                   │
│  (左下角工具栏)                                               │
├──────────────────────────────────────────────────────────────┤
│  时间轴面板（多条轨道 + 条带，可拖动、缩放）                     │
└──────────────────────────────────────────────────────────────┘
```

### 4.2 渲染页

- **背景色**：默认 `#FFFFFF`（纯白）。
- **溢出处理**：当渲染区经过缩放/旋转后超出屏幕时，渲染页应允许内容溢出显示，不裁剪。
- **沉浸模式**：隐藏 Android 底部导航栏，最大化编辑区域；用户从底部上滑可临时唤出。

### 4.3 渲染区

- **边框**：极浅的灰色边框，例如 `#E5E5E5` / `0.5dp`，保证任何白色 HTML 背景下都能看见边界。
- **阴影**：可选的轻微投影，让渲染区在纯白背景上更立体。
- **坐标系**：
  - 原点在左上角。
  - 绘制浅灰色 X/Y 轴线（`#DCDCDC`）。
  - 网格步长随缩放动态变化：`10px`、`50px`、`100px`。
  - 轴线上标注刻度值（`0`、`100`、`200`…）。
- **尺寸设置**：
  - 支持手动输入宽、高。
  - 下拉选择常用比例与分辨率。
  - 修改尺寸时，所有层的 `viewport` 同步更新。

### 4.4 左下角工具栏

- **位置**：屏幕左下角，距离边缘 `16dp`。
- **样式**：三个圆形/圆角矩形按钮，纯灰色图标（`#9E9E9E`），无文字，激活时颜色加深（`#616161`）。
- **图标**：
  - 🔍 **放大镜**：进入「搜索/浏览模式」。
  - ✏️ **铅笔**：进入「更改/编辑模式」。
  - 📷 **摄像机**：进入「摄像机模式」，开始录制渲染区内的动画。

#### 摄像机工具行为

- 点击摄像机图标后，工具栏变为录制控制条：
  - 红色圆点「开始/停止录制」按钮。
  - 显示当前录制时长 `00:00:00`。
  - 可选分辨率、帧率、输出格式（MP4 / GIF / WebM）。
- 开始录制前，编辑器自动将视图重置为「1:1 适配渲染区」，避免搜索模式下的缩放/旋转被录进视频。
- 录制范围**严格限定在渲染区矩形内**；渲染区外的白色背景、网格、坐标系均不入镜。
- 录制过程可实时预览，但暂停编辑操作以避免误触。
- 停止后弹出预览与保存选项。

### 4.5 右侧面板

- **属性面板**：调整选中元素或选中层的 `left/top/width/height/opacity/rotate/scale` 等。
- **图层面板**：列出所有 HTML 层，支持：
  - 调整层号（拖拽排序）。
  - 显隐、锁定、重命名。
  - 修改透明度与混合模式。
- **时间轴面板**（位于底部）：多层轨道、条带、播放头、缩放/平移。
- 所有面板默认可滑动收起，留出最大渲染区。

---

## 5. 三种操作模式

### 5.1 搜索模式（浏览/手势模式）

> 图标：放大镜。该模式下用户像操作地图一样观察整个渲染页。

- **单指拖动**：平移整个渲染区。
- **双指捏合**：缩放渲染区，范围 `0.1x ~ 5.0x`。
- **双指旋转**：以屏幕中心或双指中点为支点旋转渲染区，范围 `-180° ~ 180°`。
- **双击**：还原为 `1:1`、无旋转、居中显示。
- **事件拦截**：所有手势被 Flutter 的 `GestureDetector` 捕获，不传递给 WebView。

### 5.2 更改模式（编辑模式）

> 图标：铅笔。该模式下用户直接与当前激活的 HTML 层内容交互。

- **点击**：通过 WebView 注入的 JS 脚本命中 DOM 元素，显示选中框（marching ants）。
- **拖动**：移动选中的元素，实时更新 CSS `transform/left/top`。
- **缩放/旋转手柄**：选中元素四角出现控制点，拖动可缩放、旋转。
- **文本编辑**：双击文本元素唤起软键盘，直接修改内容。
- **属性输入**：右侧面板同步显示并允许精确输入。
- **新增元素**：工具栏可扩展按钮，插入矩形、文本、图片、SVG 等（生成对应 HTML）。
- **层切换**：在图层面板中选择要编辑的层，渲染区高亮当前层，其他层变暗或半透明。

### 5.3 摄像机模式（录制模式）

> 图标：摄像机。该模式下只进行录制控制，禁止编辑和搜索手势。

- 渲染区显示「REC」角标与录制安全框。
- 点击红色按钮开始/停止录制。
- 录制时时间轴自动播放，摄像机逐帧捕获渲染区画面。
- 退出摄像机模式时，若正在录制则自动停止并保存草稿。

---

## 6. HTML 导入与多层渲染

### 6.1 导入流程

1. 用户点击「导入」按钮。
2. 调用 `file_picker` 选择 `.html`、`.htm`，或选择一个包含 `index.html` 的文件夹/zip。
3. 解析 HTML，提取 `<link>`、`<script>`、`<img>`、`<style>` 等外部资源引用。
4. 将资源路径映射到本地临时目录。
5. 每个导入的 HTML 自动创建为一个新层：
   - 默认层号 = 当前最大层号 + 1。
   - 默认在时间轴上生成一条从 `0ms` 到 `5000ms` 的条带。

### 6.2 多层合成方案

推荐采用**单 WebView 多容器**方案，兼顾性能与隔离性：

- 在 WebView 内部维护一个根文档，背景透明。
- 每个层对应一个 `<iframe>` 或 `<div class="animl-layer">`：
  - `z-index: 层号`
  - `position: absolute`
  - `left: 0; top: 0`
  - `width/height` 与渲染区一致
- 层号越小，`z-index` 越小，渲染越靠下；层号越大，越靠上，覆盖下方内容。
- 时间轴通过注入 JS 控制每一层的 `display/visibility/opacity`，实现条带开始/结束效果。

### 6.3 WebView 要求

- 启用 `JavaScriptCanOpenWindowsAutomatically: false`。
- 允许本地文件访问。
- 注入一个全局 `window._AniML` 对象，用于与 Flutter 通信：
  - `selectElement(layerId, selector)`：在指定层中选中元素。
  - `getElementStyles(layerId)`：返回选中元素的 CSS 属性。
  - `setElementStyles(layerId, styles)`：更新 CSS 属性。
  - `setLayerVisible(layerId, visible)`：显隐某一层。
  - `setLayerZIndex(layerId, zIndex)`：调整层叠顺序。

### 6.4 安全沙箱

- 禁止 HTML 自动跳转、弹窗。
- 动画中使用的 `fetch/XHR` 仅允许本地资源或明确配置的白名单。
- 不执行未经验证的外部脚本（导入时可选净化处理）。

---

## 7. 动画制作

### 7.1 动画表达方式

- **CSS Keyframes**：编辑器生成 `@keyframes`，绑定到元素 class。
- **CSS Transitions**：通过属性变化触发。
- **JS 动画**：针对复杂曲线或逐帧控制，注入 `requestAnimationFrame` 脚本。
- **条带控制**：通过显隐/透明度变化让 HTML 在指定时间段出现或消失。

### 7.2 时间轴条带（Timeline Clip）

> 类似视频剪辑软件底部的时间线，但条带上不显示 HTML 画面，只显示层的活动时间范围。

- **轨道（Track）**：每个层对应一条水平轨道，轨道顺序与层号一致。
- **条带（Clip）**：轨道上的一段彩色矩形，表示该层在某一时间段内可见/活跃。
  - 条带左边缘 = 开始时间。
  - 条带右边缘 = 结束时间。
  - 条带中间显示层名称或缩略标签。
- **拖动**：
  - 拖动整条带：平移开始/结束时间，保持时长不变。
  - 拖动边缘：改变开始或结束时间。
- **重叠规则**：
  - **同一条轨道上的条带不允许重叠**，拖动时自动吸附或阻止。
  - 不同轨道的条带可以重叠，实现上下层叠加效果。
- **多个条带**：一个 HTML 层可以在设置里拆分成多条带（例如闪现、循环），拆分时同轨道仍不能重叠。
- **播放头**：垂直红线，指示当前预览/录制时间点；支持播放、暂停、循环、逐帧前进/后退。

### 7.3 摄像机录制

- **录制范围**：仅渲染区矩形内的像素。
- **录制分辨率**：默认等于渲染区像素大小（如 `1920×1080`），可降级以控制文件体积。
- **帧率**：支持 `24fps`、`30fps`、`60fps`。
- **输出格式**：
  - `MP4`（H.264，默认）
  - `WebM`（VP9，可选）
  - `GIF`（短动画分享）
- **录制流程**：
  1. 进入摄像机模式。
  2. 自动重置视图到 1:1。
  3. 播放头回到 `0ms`。
  4. 点击录制，时间轴播放，逐帧捕获渲染区。
  5. 点击停止，编码输出视频。
  6. 预览并保存到相册/指定目录。

### 7.4 资源管理

- 项目文件格式：`.animl_project`（实际为 JSON 文件）。
- 项目目录结构：

```
MyProject.animl/
├── project.json          # 项目元数据、渲染区配置、层、时间轴、摄像机设置
├── layers/               # 每个 HTML 层独立保存
│   ├── layer_0000/
│   │   ├── index.html
│   │   └── assets/
│   ├── layer_0001/
│   │   ├── index.html
│   │   └── assets/
│   └── ...
├── exports/              # 导出的 HTML / 视频
│   ├── output.html
│   └── output.mp4
└── thumbnails/
    └── preview.png
```

---

## 8. 数据模型

### 8.1 项目配置（ProjectConfig）

```json
{
  "name": "未命名项目",
  "version": "1.0.0",
  "canvas": {
    "width": 1920,
    "height": 1080,
    "backgroundColor": "#FFFFFF",
    "showGrid": true,
    "gridSize": 50
  },
  "viewport": {
    "scale": 1.0,
    "offsetX": 0.0,
    "offsetY": 0.0,
    "rotation": 0.0
  },
  "layers": [
    {
      "id": "layer_0000",
      "name": "背景",
      "index": 0,
      "visible": true,
      "locked": false,
      "opacity": 1.0,
      "blendMode": "normal",
      "source": "layers/layer_0000/index.html"
    },
    {
      "id": "layer_0001",
      "name": "角色",
      "index": 1,
      "visible": true,
      "locked": false,
      "opacity": 1.0,
      "blendMode": "normal",
      "source": "layers/layer_0001/index.html"
    }
  ],
  "timeline": {
    "duration": 10000,
    "tracks": [
      {
        "layerId": "layer_0000",
        "clips": [
          {"start": 0, "end": 10000}
        ]
      },
      {
        "layerId": "layer_0001",
        "clips": [
          {"start": 2000, "end": 8000}
        ]
      }
    ]
  },
  "camera": {
    "format": "mp4",
    "fps": 30,
    "resolution": {"width": 1920, "height": 1080}
  }
}
```

### 8.2 状态管理划分

- `CanvasController`：渲染区尺寸、背景、网格、坐标系显示。
- `ViewportController`：缩放、平移、旋转（仅在搜索模式下响应手势）。
- `LayerController`：层的增删改查、排序、显隐、锁定、透明度。
- `WebViewController`：加载 HTML、注入 JS、与 DOM 通信。
- `TimelineController`：时间轴播放、条带编辑、轨道管理。
- `CameraController`：录制控制、帧捕获、视频编码。
- `ModeController`：当前模式（搜索/更改/摄像机）、工具状态。
- `ProjectController`：项目文件读写、资源索引、导出编译。

---

## 9. 手势与事件路由

- 渲染区上层覆盖一个 `GestureDetector`。
- **搜索模式**：返回 `true` 消费事件，WebView 不可交互。
- **更改模式**：`GestureDetector` 只处理选框/空白区域手势；对元素本身的点击/拖动交给 WebView 内部 JS 处理。
- **摄像机模式**：禁用搜索与编辑手势，仅响应录制按钮。
- 通过 `IgnorePointer` 或 `HitTestBehavior.translucent` 灵活切换。

---

## 10. 导出/编译

### 10.1 导出目标

- **HTML**：将多层动画合并为单一自包含的 `.html` 文件。
- **视频**：通过摄像机录制渲染区，输出 MP4 / WebM / GIF。
- **项目包**：`.animl_project` 目录，保留完整编辑信息。

### 10.2 HTML 编译步骤

1. 读取所有层的 `index.html`。
2. 将外部 CSS/JS/图片等资源内联。
3. 根据时间轴条带生成显隐控制动画（CSS 或 JS）。
4. 按 `z-index` 顺序将所有层容器合并到一个根 HTML 中。
5. 清理开发期注入的 `_AniML` 脚本与选中框样式。
6. 输出最终文件。

### 10.3 视频导出步骤

1. 进入静默录制状态（不显示 REC 角标）。
2. 按设定帧率与分辨率逐帧捕获渲染区。
3. 使用 MediaCodec / FFmpeg 编码为指定格式。
4. 写入 `exports/` 或用户选择的目录。

---

## 11. 性能与兼容性

- WebView 启用硬件加速。
- 复杂动画优先使用 CSS `transform` 和 `opacity`，避免触发重排。
- 对 JS 调用做防抖/节流，降低 Bridge 开销。
- 摄像机录制时减少 UI 刷新，确保帧率稳定。
- 支持 Android 8.0+（API 26+）。
- 屏幕尺寸适配：针对手机横屏优化，最小宽度 600dp。

---

## 12. 权限

| 权限 | 用途 |
|------|------|
| `INTERNET` | WebView 本地文件加载可能依赖（低版本 Android）。 |
| `READ_EXTERNAL_STORAGE` | 导入本地 HTML/资源。 |
| `WRITE_EXTERNAL_STORAGE` / `MANAGE_EXTERNAL_STORAGE` | 导出 HTML/视频到用户可见目录（视 Android 版本而定）。 |
| `RECORD_AUDIO` | 若摄像机需要录制系统音频（可选）。 |
| `WAKE_LOCK` | 长时间预览/录制动画时保持屏幕常亮。 |

---

## 13. 项目目录结构

```
animl/
├── android/                       # Android 原生配置
├── ios/                           # iOS 配置（预留）
├── lib/
│   ├── main.dart
│   ├── app.dart
│   ├── models/
│   │   ├── project_config.dart
│   │   ├── layer.dart
│   │   ├── clip.dart
│   │   └── asset_item.dart
│   ├── controllers/
│   │   ├── canvas_controller.dart
│   │   ├── viewport_controller.dart
│   │   ├── layer_controller.dart
│   │   ├── webview_controller.dart
│   │   ├── timeline_controller.dart
│   │   ├── camera_controller.dart
│   │   ├── mode_controller.dart
│   │   └── project_controller.dart
│   ├── ui/
│   │   ├── render_page.dart
│   │   ├── render_area.dart
│   │   ├── bottom_toolbar.dart
│   │   ├── property_panel.dart
│   │   ├── layer_panel.dart
│   │   └── timeline_panel.dart
│   ├── services/
│   │   ├── html_importer.dart
│   │   ├── html_compiler.dart
│   │   ├── camera_recorder.dart
│   │   └── storage_service.dart
│   └── utils/
│       ├── constants.dart
│       └── js_bridge.dart
├── assets/
│   └── icons/
│       ├── search.svg
│       ├── edit.svg
│       └── camera.svg
├── pubspec.yaml
└── DESIGN.md
```

---

## 14. 开发里程碑

| 阶段 | 目标 |
|------|------|
| **MVP** | 横屏渲染页、渲染区尺寸/比例可调、HTML 导入为层、搜索/更改/摄像机模式、坐标系、纯灰工具栏、基础 HTML 导出。 |
| **V1.0** | 图层面板、层叠顺序、时间轴条带、同轨道防重叠、摄像机录制 MP4、项目文件保存/打开。 |
| **V1.5** | 条带分割/循环、元素选中与属性编辑、撤销/重做、GIF/WebM 导出、缓动函数库。 |
| **V2.0** | 组件模板、音频层、预览播放器、云同步、APK 发布。 |

---

## 15. 风险与待确认事项

1. **多层性能**：多个 HTML 层同时运行可能对 WebView 造成压力，需要限制同时活跃的层数或采用懒加载。
2. **摄像机同步**：录制帧率需与时间轴播放严格同步，避免掉帧或音画不同步。
3. **坐标系原点**：本文假设为渲染区左上角。若后续需求改为中心原点，需调整网格、属性面板和导出逻辑。
4. **条带重叠策略**：同轨道禁止重叠；跨轨道重叠是设计预期，需在 UI 中明确区分。
5. **横屏锁定**：是否允许用户临时切换竖屏查看效果，还是严格锁定横屏？

---

## 16. 结语

AniML 的核心理念是「**用 HTML 做动画，像剪辑视频一样简单**」。通过 Flutter 提供的高性能 UI、WebView 的 HTML 渲染能力，以及层、时间轴、摄像机等动画制作常见概念，用户在手机上即可导入、层叠、剪辑并导出独立的 HTML 动画或视频。
